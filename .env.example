# Postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=caretaker_app
DATABASE_URL=postgresql://postgres:your_password_here@localhost:5432/caretaker_app

# Server
PORT=3333

# JWT (Generate these using: openssl rand -hex 16)
SIGNUP_TOKEN_SECRET=your_signup_secret_here
SESSION_TOKEN_SECRET=your_session_secret_here

# Frontend Deployment (These are public identifiers)
AWS_CARETAKER_CLIENT_BUCKET_NAME=caretaker.center
AWS_CLOUDFRONT_DISTRIBUTION_ID=E3D67455S5G1DN

# Note: Replace sensitive values with your own secure values
# Never commit the actual .env file to version control 